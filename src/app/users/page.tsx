'use client'

// react
import React, { useCallback, useRef } from 'react'

// components
import { AppBaseButton, AppBaseLoading, AppBaseTextInput } from '@/components'

const Users = () => {
  return (
    <div className="flex flex-col">
      <span>users</span>
    </div>
  )
}

export default Users
